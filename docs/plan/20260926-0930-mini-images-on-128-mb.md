# 20260926-0930-mini-images-on-128-mb A mini x64 and arm64 image that runs from 128 MB of flash

- **status**: pending
- **createdAt**: 2026-09-26 09:30

## Context

The user asked (2026-09-26) for a mini build for x64 and arm64: no container engine, space
compressed to the extreme, and "the goal is that it still runs on 128 MB of flash". Measured at
`mica-build` `ebcc6e8` and `mica-core` `db3500b`, on the local `uefi-x64-dev` build.

### Where the space goes today

| Item | Today | Source |
|---|---|---|
| Disk image, uefi-x64 | 1,881,145,344 bytes (1794 MiB) | `boards/uefi-x64/layout.tsv`: ESP 512 MiB, SYSTEM 1 GiB, DATA 256 MiB |
| Root, full dev product | 177 MiB composed, 65.8 MB squashfs (zstd 19, 128K) | `rootfs-report.runtime.json` |
| Root without podman | 115 MiB composed, 42.8 MB squashfs | the same root with podman's files removed, repacked |
| Root, the floor (no feature) | 84 MiB composed, 31.1 MB squashfs | the same, without micad, apid and mqtt |
| UKI | 15.5 MiB: `.linux` 15,143,936 B, `.initrd` 969,786 B (2.6 MB unpacked) | `objdump -h` of the UKI |
| SYSTEM in use | 63 MiB of 1 GiB: one root | `debugfs rdump` of `system.img` |
| ESP in use | 16 MiB of 512 MiB | the image's `esp-tree` |

Compression, measured on the root without podman:

| Options | Size |
|---|---|
| zstd 19, 128K blocks (shipped) | 42.8 MB |
| zstd 22, 1M blocks | 38.2 MB |
| xz, 1M blocks, BCJ filter | 34.0 MB |

The largest packages in the root without podman (composed bytes):
- mica-apid 12.8 MiB
- libc6 10.9
- micad 8.4
- libssl3 7.3
- libsystemd-shared 6.6
- coreutils 6.6
- systemd 6.1
- mica-mqtt-broker 5.4
- mica-mqttd 5.0
- util-linux 4.1
- mica-deploy 3.4
- busybox 2.3

### What forbids 128 MB today, independent of the image

Three reserves, written as constants:

- `mica-build:src/image/file-layout.ts` `checkCapacity`: SYSTEM must hold two deployments plus 128
  MiB. An ESP must hold two boot artifacts plus 64 MiB.
- `mica-core:crates/mica-deploy/src/deployments.rs:1055`: installing into SYSTEM requires 128 MiB
  free beyond the deployment.
- `mica-core:crates/mica-deploy/src/acquisition.rs:100`: every download into the DATA workspace
  requires 128 MiB free beyond the bytes it reserves.

These reserves alone are 320 MiB. A 128 MB part holds 122 MiB.

## Proposal

### The budget

For 128 MB (122 MiB), keeping A/B updates:

| Area | Size | Holds |
|---|---|---|
| GPT and alignment | 1 MiB | |
| ESP (FAT16) | 24 MiB | two UKIs of at most 10 MiB each, and systemd-boot |
| SYSTEM (ext4) | 60 MiB | two roots of at most 26 MB each, their verity data, ext4 overhead, a 4 MiB reserve |
| DATA | about 37 MiB at first boot; the rest of a larger part after growth | state, and the staging of one update |

The sizes are what the measurements below have to confirm, not a commitment. Three targets follow
from them:

- **The kernel at most 9 MB**, from 15.1 MB. The generic config builds the world in, because a
  verity root cannot load modules before the root is up. A mini kernel config keeps the floor
  (`common/kernel/mica-required.fragment`) and the virtual and generic UEFI drivers, and nothing
  else, compressed with xz.
- **The root at most 26 MB.** The root without podman is 34.0 MB even under xz, so about 8 MB has
  to go, in this order of measured size:
  - libc6's charset converters (gconv), which nothing in the root loads;
  - coreutils and part of util-linux, whose commands busybox already provides, where no unit or
    script needs the GNU behaviour;
  - `mica-apid`'s embedded UI assets, compressed or served from DATA;
  - mqtt, if the product does not need it (open question 2).
- **The reserves as policy, not constants.** They become facts of the board, in the signed boot
  policy's `board` section that `db3500b` introduced (and in `layout.tsv` for the build side). A
  board declares its SYSTEM, ESP and DATA staging reserves, and the device and the build both hold
  them. A generic board keeps 128 MiB; the mini boards declare a few MiB. On the DATA side the
  staging bound is what an update of this product actually needs. The delta transfer that
  `mica-core` is building (`bccab41`: an object assembled from chunks the device already holds)
  lowers it further.

### Phases

- **P0, the measurement** (`mica-build`, nothing shipped). A trial kernel with the mini config,
  built and packed into a UKI, and a trial root with the trims above, packed with xz. Their sizes
  decide the budget and which features fit. Measured, then reported, before anything below starts.
- **P1, `mica-core`: the reserves in the policy.** The `board` section gains the three reserves.
  `deployments.rs` and `acquisition.rs` read them in place of the constants. Tests: an install and
  a download refused at the declared reserve, and accepted just above it.
- **P2, `mica-build`: the mini boards.** Two new boards, `uefi-x64-mini` and `uefi-arm64-mini`, with:
  - the mini layout;
  - FAT16 in the ESP role;
  - the mini kernel config;
  - their reserves;
  - per-product squashfs compression (a product key, `xz` for the mini products);
  - the root trims as declared drops.

  Products `uefi-x64-mini` and `uefi-arm64-mini`: no `containers`. `checkCapacity` reads the
  declared reserves. Because they are new boards, this needs `db3500b` released and `mica-build`'s
  counterpart commit first (the running device refuses an unknown board name today), and the fleet
  plan (`20260926-0800-fleet-without-a-board-table`) for updates through the platform.
- **P3, the proof.** The lifecycle suite in QEMU on a 128 MB disk, x64 natively and arm64 under
  emulation:
  - first boot and DATA growth;
  - an update into the other slot;
  - a rollback.

  This runs as part of the products' release path, as the dev products' lifecycle stage does
  today.

## Risks

- **A root of 26 MB may not hold micad, apid and mqtt together.** P0 answers this with numbers. The
  product can drop mqtt, or DATA can host apid's UI, before the budget moves.
- **xz reads cost CPU** on every page fault of the root. It is measurable on arm64, and P3 records
  the boot time next to the dev product's.
- **A 9 MB kernel is a second config per architecture to maintain.** It is held by the same floor
  check (`common/kernel/floor-check.sh`) the generic kernels pass.
- **FAT16 below 32 MiB.** The EFI specification allows FAT12/16 on removable media, and on fixed
  disks firmware commonly reads them. QEMU's OVMF and the arm64 AAVMF do, which P3 exercises; real
  hardware is a per-board qualification.

## Scope

In:
- the three reserves (`mica-core`, `mica-build`);
- the mini kernel configs;
- the two mini boards and products;
- per-product compression;
- the root trims as declarations;
- the 128 MB lifecycle proof.

Out:
- RAM limits (not measured here);
- the FIT boards, whose layouts are their hardware's;
- any change to the existing boards and products.

## Open questions for the user

1. Keep A/B updates within 128 MB, as budgeted? The alternative is a single root slot with updates
   by reflash, which halves SYSTEM but loses rollback.
2. Which features must the mini products keep: micad and apid (management), mqtt? Each is measured
   in P0.
3. Boot time against size: xz everywhere, or zstd 22 where the difference is small?

## Progress

### 2026-09-26: the floor measured, and the network's price

On the Base root of release 20260920-0832 (amd64), by ELF closure and `du`:

| Set | Size |
|---|---|
| systemd's own floor: PID1, udevd, journald, systemctl, and every library they load | 0.6 MB of binaries, 19.0 MB of libraries |
| of which libcrypto 6.4, libsystemd-shared 4.3, libsystemd-core 2.4, libc 1.9 | 15.0 MB |
| Full network configuration: networkd 1.78, resolved 0.55, networkctl and resolvectl 0.29, timesyncd 0.06, udev with its rules 0.67 | 3.35 MB |
| nftables with libnftables, libnftnl and libmnl | 1.23 MB |

`systemd-networkd` loads **no library PID1 does not already load**, so DHCP, static addresses,
VLAN, bridge, bond, wireguard and macvlan cost 3.35 MB in total and nothing per feature.

A distroless-shaped root, with busybox as `/bin/sh` and no bash, coreutils, passwd or gconv:

| Layer | Size |
|---|---|
| systemd floor with its libraries | 19.6 MB |
| full network configuration | 3.4 MB |
| busybox as `/bin/sh` and the basic commands | 2.2 MB |
| what the payload scripts need beside busybox: findmnt, e2fsck, resize2fs, setquota | 1.1 MB |
| dbus | 0.7 MB |
| dropbear | 1.5 MB |
| console login: login, unix_chkpwd, PAM | 1.2 MB |
| nftables | 1.2 MB |
| the packages' copyright files | 1.8 MB |
| **total** | **about 33 MB composed** |

That is about 11 to 12 MB under zstd 19 and about 10 MB under xz, against the 84 MiB composed and
31.1 MB squashfs measured for the floor root above: the difference is gconv (7.7 MB), the GNU
command set busybox already provides (4.3 MB), the passwd suite (2.5 MB), the locale tools
(1.3 MB) and zoneinfo beyond UTC (0.6 MB).

A root with no shell at all is not reachable without work in `mica-core`: the eleven payload
scripts of `mica-system` are `/bin/sh`, and beside the commands busybox provides they call
`findmnt`, `setquota`, `dropbearkey`, `systemctl` and `systemd-repart`. Porting them into micad
and mica-deploy would buy about 3 MB and cost the console its tools; busybox stays (decision 3).

### 2026-09-26: wireless, built and measured

wpa_supplicant 2.10 and hostapd 2.10 (the upstream of trixie's 2:2.10-24), built in a trixie
container with nl80211, libnl-3 and libnl-genl-3 linked statically, the control interface, the
file backend, AP mode, bgscan and PMF, and nothing else (no D-Bus, no PC/SC, no readline, no P2P,
WPS or mesh), stripped:

| Build | Size | Shared libraries |
|---|---|---|
| wpa_supplicant, internal crypto, WPA2-PSK | 1126 KiB | libc only |
| wpa_supplicant, internal crypto, with SAE (WPA3) | does not link | the internal crypto has no bignum for SAE |
| wpa_supplicant, OpenSSL, WPA2 and WPA3 | 1196 KiB | libcrypto, libz, libzstd |
| wpa_supplicant, OpenSSL, WPA2, WPA3 and enterprise EAP (PEAP, TTLS, TLS, MSCHAPv2) | 1385 KiB | adds libssl |
| hostapd, OpenSSL, WPA2 and WPA3 AP, no RADIUS | 844 KiB | libcrypto, libz, libzstd |
| wpa_cli, without readline | 120 KiB | libc only |
| Debian's wpa_supplicant 2:2.10-24, for comparison | 3321 KiB | libnl-3, libnl-genl-3, libnl-route-3, libpcsclite, libdbus, libssl, libcrypto, libz, libzstd, libm |
| Debian's hostapd, for comparison | 1993 KiB | libnl x3, libssl, libcrypto |

Every library the OpenSSL builds load is already in the root: libcrypto and libzstd are in
systemd's own closure, libz is libcrypto's, and libssl is resolved's. The two OpenSSL builds together
are about 2.0 MB and add no library, against about 7.0 MB and five pinned packages today.

micad needs no change for them. What it writes and sends today is inside the minimal builds:

- `wifi_client.rs`: `key_mgmt=NONE` and `WPA-PSK`, `psk`, `update_config`, `ctrl_interface`;
- `wifi_ap.rs`: hostapd's `wpa_key_mgmt=WPA-PSK`, `hw_mode=g`, `ieee80211d`, `country_code`,
  `wmm_enabled`, `channel`, `ignore_broadcast_ssid`;
- `network_state.rs`: scanning and status already go through wpa_supplicant's control socket
  (`SCAN`, `SCAN_RESULTS`, `SIGNAL_POLL`, `STATUS`), which the minimal build keeps.

iwd, by contrast (daemon 0.97 MB and libell 0.60 MB), adds libell, needs
`CONFIG_CRYPTO_USER_API_HASH` and `CONFIG_CRYPTO_USER_API_SKCIPHER` in the kernel, and would replace
all three of micad's wireless paths with its D-Bus API. It is not smaller than the minimal builds
in this root and costs more.

Compatibility, in three layers:

- **The build.** Against Debian's `debian/config/wpasupplicant/linux`, the minimal build drops
  WEXT, wired 802.1X, every EAP method (the EAP variant restores PEAP, TTLS, TLS, MSCHAPv2, MD5 and
  GTC), WEP, WPS, OWE, 802.11r, WNM and MBO, Hotspot 2.0 and interworking, P2P, mesh, TDLS, FILS,
  MACsec, smartcard EAP-SIM/AKA and Suite B. It keeps open networks, WPA and WPA2-PSK, WPA3-SAE
  (with the OpenSSL build), PMF, hidden SSIDs, bgscan and AP mode. 802.11n/ac/ax rates are the
  driver's, not the supplicant's.
- **micad.** `wifi_client.rs` writes `key_mgmt=WPA-PSK` or `NONE` only, so a WPA3-only network,
  which every 6 GHz network is, cannot be joined today with any build. `key_mgmt=WPA-PSK SAE` with
  `ieee80211w=1` joins WPA2, WPA3 and transition networks from one block. `wifi_ap.rs` writes
  `hw_mode=g` without `ieee80211n`, so the AP is 802.11g.
- **The kernel.** The pins are 6.1 (cx3576), 6.12.y (s905x5m) and 6.12.107 (uefi and mini); nl80211
  control port and SAE external authentication date from 4.17, so no kernel constrains the build.
  cx3576's AIC8800 and s905x5m's Seekwave SWT6621 are cfg80211 drivers with their firmware shipped;
  the Seekwave driver advertises station, AP, ad-hoc and monitor and has an SAE path, and its
  private WEXT table (`skw_iw.c`) is for vendor ioctls, not for the supplicant. uefi-x64 builds the
  mainline drivers but ships no firmware (`BOARD_FIRMWARE_FILES=""`), so most cards will not come
  up; uefi-arm64 and both mini kernels have no cfg80211 at all.

The packages would be built here like mica-busybox: the pinned Debian source, so its security
patches come with it, a committed `.config`, and version locking.

## Annotations

2026-09-26, the user's answers to the three open questions:

1. **Plan for 128 MB with A/B updates**, as budgeted. micad and apid are the user's own work,
   in parallel with this plan.
2. **Wireless**: the user asked whether a smaller option exists. Measured above: minimal builds of
   wpa_supplicant and hostapd against the root's OpenSSL are about 2.0 MB with no new library and
   no micad change; iwd is larger in this root and needs micad's wireless paths rewritten.
   Decided: the minimal builds, from upstream hostap (2.12), as mica-system-base's
   mica-wifi and mica-wifi-ap (mica-system-base task 20260926-0904-mica-wifi).
3. **busybox stays** as `/bin/sh` and the basic command set. No shell-less root.
